<script setup lang="ts">
import Breadcrumbs from '@/components/Breadcrumbs.vue';
import { SidebarTrigger } from '@/components/ui/sidebar';
import WorkspaceToolbarControls from '@/components/WorkspaceToolbarControls.vue';
import type { BreadcrumbItem } from '@/types';

withDefaults(
    defineProps<{
        breadcrumbs?: BreadcrumbItem[];
    }>(),
    {
        breadcrumbs: () => [],
    }
);
</script>

<template>
    <header
        class="flex h-16 min-w-0 shrink-0 items-center justify-between gap-2 border-b border-sidebar-border/70 bg-background/80 px-3 backdrop-blur transition-[width,height] ease-linear group-has-data-[collapsible=icon]/sidebar-wrapper:h-12 sm:px-4"
    >
        <div class="flex min-w-0 items-center gap-2">
            <SidebarTrigger
                class="shrink-0 rounded-full border border-sidebar-border bg-background shadow-sm"
            />
            <template v-if="breadcrumbs && breadcrumbs.length > 0">
                <Breadcrumbs :breadcrumbs="breadcrumbs" />
            </template>
        </div>

        <WorkspaceToolbarControls />
    </header>
</template>
