import savedQueries from './saved-queries'
import modulePins from './module-pins'
const dashboard = {
    savedQueries: Object.assign(savedQueries, savedQueries),
modulePins: Object.assign(modulePins, modulePins),
}

export default dashboard